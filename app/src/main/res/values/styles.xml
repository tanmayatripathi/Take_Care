<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="AppTheme_Teal" parent="android:Theme.Material">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <item name="android:colorAccent">@color/accent</item>
    </style>
    <style name="TimePickerStyle" parent="@android:style/Widget.Material.TimePicker">
        <item name="android:headerBackground">@color/primary</item>
        <item name="android:headerTimeTextAppearance">@style/TimeLabelTextAppearance</item>
        <item name="android:headerAmPmTextAppearance">@style/AmPmTextAppearance</item>
        <item name="android:numbersBackgroundColor">@color/white</item>
        <item name="android:numbersTextColor">@color/primary</item>
        <item name="android:amPmTextColor">@color/primary</item>
    </style>
    <style name="TimeLabelTextAppearance">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">60sp</item>
    </style>
    <style name="AmPmTextAppearance">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
